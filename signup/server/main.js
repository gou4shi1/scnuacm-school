import { Meteor } from 'meteor/meteor';

import '../imports/api/geetest';
import '../imports/api/members';
import '../imports/api/teams';

Meteor.startup(() => {
  // code to run on server at startup
});
